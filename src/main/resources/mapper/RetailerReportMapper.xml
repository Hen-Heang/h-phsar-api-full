<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.henheang.hphsar.repository.RetailerReportRepository">

    <!--
        ResultMap for getCategoryNameAndTotalItem
        Maps categoryName and totalItem columns to CategoryNameAndTotalOfQty fields.
    -->
    <resultMap id="CategoryNameAndTotalOfQtyResultMap"
               type="com.henheang.hphsar.model.retailer.report.CategoryNameAndTotalOfQty">
        <result property="categoryName" column="categoryName"/>
        <result property="totalItem"    column="totalItem"/>
    </resultMap>

    <!-- ═══════════════════════════════════════════════════════════════
         SAME YEAR QUERIES (used when startYear == endYear)
         ═══════════════════════════════════════════════════════════════ -->

    <!--
        Count all orders placed by this retailer in a specific month/year (any active status).
        Used for: total order count in the same-year report.
    -->
    <select id="getTotalMonthlyOrderByCurrentMonth" resultType="int">
        SELECT COALESCE(COUNT(*), 0)
        FROM tb_order
        WHERE retailer_account_id = #{currentUserId}
          AND status_id IN (1, 2, 3, 4, 5, 6)
          AND EXTRACT(YEAR  FROM created_date) = #{year}
          AND EXTRACT(MONTH FROM created_date) = #{month}
    </select>

    <!--
        Count orders by a specific status (5 = accepted, 6 = rejected) in a given month/year.
        Called twice per month: once with statusId=5, once with statusId=6.
    -->
    <select id="getTotalRejectedAndAccepted" resultType="int">
        SELECT COALESCE(COUNT(*), 0)
        FROM tb_order
        WHERE retailer_account_id = #{currentUserId}
          AND status_id            = #{statusId}
          AND EXTRACT(YEAR  FROM created_date) = #{year}
          AND EXTRACT(MONTH FROM created_date) = #{month}
    </select>

    <!--
        Sum of all item quantities in accepted orders for this retailer in a given month/year.
    -->
    <select id="getTotalQuantityOrder" resultType="int">
        SELECT COALESCE(SUM(od.qty), 0)
        FROM tb_order o
                 JOIN tb_order_detail od ON o.id = od.order_id
        WHERE o.retailer_account_id = #{currentUserId}
          AND o.status_id           = #{statusId}
          AND EXTRACT(YEAR  FROM o.created_date) = #{year}
          AND EXTRACT(MONTH FROM o.created_date) = #{month}
    </select>

    <!--
        Category names of products in orders with the given status, month, and year.
        Used to build the category label list on the report.
    -->
    <select id="getCategoryNameOrder" resultType="String">
        SELECT c.name
        FROM tb_order o
                 JOIN tb_order_detail od      ON o.id              = od.order_id
                 JOIN tb_store_product_detail spd ON od.store_product_id = spd.id
                 JOIN tb_category c           ON spd.category_id   = c.id
        WHERE o.retailer_account_id = #{currentUserId}
          AND o.status_id           = #{statusId}
          AND EXTRACT(YEAR  FROM o.created_date) = #{year}
          AND EXTRACT(MONTH FROM o.created_date) = #{month}
    </select>

    <!--
        List of store IDs that this retailer placed orders at in a given month/year.
        The service counts distinct values to get unique shop count.
    -->
    <select id="getPurchasedShopOrdered" resultType="int">
        SELECT o.store_id
        FROM tb_order o
        WHERE o.retailer_account_id = #{currentUserId}
          AND o.status_id           = #{statusId}
          AND EXTRACT(YEAR  FROM o.created_date) = #{year}
          AND EXTRACT(MONTH FROM o.created_date) = #{month}
    </select>

    <!--
        Total money spent (qty × unit_price) by this retailer for accepted orders in a given month/year.
    -->
    <select id="getTotalExpense" resultType="int">
        SELECT COALESCE(SUM(od.qty * od.unit_price), 0)
        FROM tb_order o
                 JOIN tb_order_detail od ON o.id = od.order_id
        WHERE o.retailer_account_id = #{currentUserId}
          AND o.status_id           = #{statusId}
          AND EXTRACT(YEAR  FROM o.created_date) = #{year}
          AND EXTRACT(MONTH FROM o.created_date) = #{month}
    </select>

    <!--
        Count of store ratings submitted by this retailer in a given month/year.
    -->
    <select id="getTotalRatingStore" resultType="int">
        SELECT COALESCE(COUNT(*), 0)
        FROM tb_rating_detail
        WHERE retailer_account_id             = #{currentUserId}
          AND EXTRACT(YEAR  FROM created_date) = #{year}
          AND EXTRACT(MONTH FROM created_date) = #{month}
    </select>

    <!--
        Total money spent for the entire year (all months).
        Used for the yearly expense summary.
    -->
    <select id="getTotalYearlyExpense" resultType="double">
        SELECT COALESCE(SUM(od.qty * od.unit_price), 0.0)
        FROM tb_order o
                 JOIN tb_order_detail od ON o.id = od.order_id
        WHERE o.retailer_account_id             = #{currentUserId}
          AND o.status_id                        = #{statusId}
          AND EXTRACT(YEAR FROM o.created_date)  = #{year}
    </select>

    <!--
        Qty per category for the given month/year — returns one Integer per category row.
        Service collects all values into List<Integer> for the chart.
    -->
    <select id="getTotalQtyInEachCategory" resultType="int">
        SELECT COALESCE(SUM(od.qty), 0)
        FROM tb_order o
                 JOIN tb_order_detail od          ON o.id              = od.order_id
                 JOIN tb_store_product_detail spd ON od.store_product_id = spd.id
        WHERE o.retailer_account_id = #{currentUserId}
          AND o.status_id           = #{statusId}
          AND EXTRACT(YEAR  FROM o.created_date) = #{year}
          AND EXTRACT(MONTH FROM o.created_date) = #{month}
        GROUP BY spd.category_id
    </select>

    <!--
        Category name + total items ordered — used for cross-year grouping.
        Service merges results across months then re-groups by category name.
    -->
    <select id="getCategoryNameAndTotalItem" resultMap="CategoryNameAndTotalOfQtyResultMap">
        SELECT c.name          AS categoryName,
               SUM(od.qty)     AS totalItem
        FROM tb_order o
                 JOIN tb_order_detail od          ON o.id              = od.order_id
                 JOIN tb_store_product_detail spd ON od.store_product_id = spd.id
                 JOIN tb_category c              ON spd.category_id    = c.id
        WHERE o.retailer_account_id = #{currentUserId}
          AND o.status_id           = #{statusId}
          AND EXTRACT(YEAR  FROM o.created_date) = #{year}
          AND EXTRACT(MONTH FROM o.created_date) = #{month}
        GROUP BY c.name
    </select>

    <!-- ═══════════════════════════════════════════════════════════════
         CROSS-YEAR QUERIES (used when startYear != endYear loop)
         These are functionally identical to the same-year queries above
         but are separate methods so the service can call them in the
         cross-year while-loop without mixing contexts.
         ═══════════════════════════════════════════════════════════════ -->

    <select id="getTotalOrderFromDifferentYear" resultType="int">
        SELECT COALESCE(COUNT(*), 0)
        FROM tb_order
        WHERE retailer_account_id = #{currentUserId}
          AND status_id IN (1, 2, 3, 4, 5, 6)
          AND EXTRACT(YEAR  FROM created_date) = #{year}
          AND EXTRACT(MONTH FROM created_date) = #{month}
    </select>

    <select id="getTotalAcceptedAndRejectedFromDifferentYear" resultType="int">
        SELECT COALESCE(COUNT(*), 0)
        FROM tb_order
        WHERE retailer_account_id = #{currentUserId}
          AND status_id            = #{statusId}
          AND EXTRACT(YEAR  FROM created_date) = #{year}
          AND EXTRACT(MONTH FROM created_date) = #{month}
    </select>

    <select id="getTotalQuantityInDifferenceYear" resultType="int">
        SELECT COALESCE(SUM(od.qty), 0)
        FROM tb_order o
                 JOIN tb_order_detail od ON o.id = od.order_id
        WHERE o.retailer_account_id = #{currentUserId}
          AND o.status_id           = #{statusId}
          AND EXTRACT(YEAR  FROM o.created_date) = #{year}
          AND EXTRACT(MONTH FROM o.created_date) = #{month}
    </select>

    <select id="getCategoryNameOrderIndDifferentYear" resultType="String">
        SELECT c.name
        FROM tb_order o
                 JOIN tb_order_detail od          ON o.id              = od.order_id
                 JOIN tb_store_product_detail spd ON od.store_product_id = spd.id
                 JOIN tb_category c              ON spd.category_id    = c.id
        WHERE o.retailer_account_id = #{currentUserId}
          AND o.status_id           = #{statusId}
          AND EXTRACT(YEAR  FROM o.created_date) = #{year}
          AND EXTRACT(MONTH FROM o.created_date) = #{month}
    </select>

    <select id="getTotalPurchasedShopDifferent" resultType="int">
        SELECT o.store_id
        FROM tb_order o
        WHERE o.retailer_account_id = #{currentUserId}
          AND o.status_id           = #{statusId}
          AND EXTRACT(YEAR  FROM o.created_date) = #{year}
          AND EXTRACT(MONTH FROM o.created_date) = #{month}
    </select>

    <select id="getTotalExpenseInDifferentYear" resultType="int">
        SELECT COALESCE(SUM(od.qty * od.unit_price), 0)
        FROM tb_order o
                 JOIN tb_order_detail od ON o.id = od.order_id
        WHERE o.retailer_account_id = #{currentUserId}
          AND o.status_id           = #{statusId}
          AND EXTRACT(YEAR  FROM o.created_date) = #{year}
          AND EXTRACT(MONTH FROM o.created_date) = #{month}
    </select>

    <select id="getRatingInDifferentYear" resultType="int">
        SELECT COALESCE(COUNT(*), 0)
        FROM tb_rating_detail
        WHERE retailer_account_id             = #{currentUserId}
          AND EXTRACT(YEAR  FROM created_date) = #{year}
          AND EXTRACT(MONTH FROM created_date) = #{month}
    </select>

    <select id="getTotalYearlyInDifferentYear" resultType="double">
        SELECT COALESCE(SUM(od.qty * od.unit_price), 0.0)
        FROM tb_order o
                 JOIN tb_order_detail od ON o.id = od.order_id
        WHERE o.retailer_account_id            = #{currentUserId}
          AND o.status_id                       = #{statusId}
          AND EXTRACT(YEAR FROM o.created_date) = #{year}
    </select>

</mapper>